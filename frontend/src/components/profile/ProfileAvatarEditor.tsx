import AvatarEditor, { type AvatarEditorProps } from '../shared/AvatarEditor';

const ProfileAvatarEditor = (props: AvatarEditorProps) => <AvatarEditor {...props} />;

export default ProfileAvatarEditor;
